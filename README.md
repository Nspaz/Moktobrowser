# Moktobrowser
TS/PW/CDP script BulletproofOctoMobilePlaywright automates Octo. startOcto POSTs headless &amp; flags (start-maximized). launch sets header Sec-CH-UA-Mobile, inits tracing (snaps/shots). CDP enables setTouchEmulation &amp; setDeviceMetrics (scale 3). sensorInterval uses specific noise math to drift DeviceOrientation &amp; Geolocation via randomWalk.
